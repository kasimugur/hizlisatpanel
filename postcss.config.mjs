const config = {
  plugins: ["@tailwindcss/postcss"],
  theme: {
  extend: {
    colors: {
      primary: '#3B82F6',   // Mavi
      success: '#10B981',   // Yeşil
      danger: '#EF4444',    // Kırmızı
      warning: '#F59E0B',   // Sarı
      base: '#F9FAFB',      // Açık zemin

    },
  
  },
},
};

export default config;

// tailwind.config.js

